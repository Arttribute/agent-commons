# Changelog

## 0.1.0

- Add the Agent Commons coding sidebar and native terminal sessions with a bundled CLI.
- Add agent selection, account sign-in, session resume, and editor explain/review actions.
- Add `agc code`, initial tasks, project instructions, and local read-only mode.
- Enforce workspace trust, safe launch arguments, local path boundaries, and explicit approvals.
- Provide public VSIX release automation and optional Marketplace publishing.
