node_modules
.turbo

# Environment files — never commit live env values; only *.example files are tracked
.env
.env.*
!.env.example
!.env.*.example

.vercel

# Supabase CLI local link state
supabase/.temp
supabase/.branches
