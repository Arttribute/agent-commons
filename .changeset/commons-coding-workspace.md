---
"@agent-commons/cli": minor
---

Add `agc code`, initial task and prompt-file support, project instructions, and local read-only mode for the VS Code coding workspace. Harden local path boundaries and approval defaults, preserve the original agent when resuming, and stop changing Git hooks at chat startup.
