---
"@agent-commons/sdk": minor
---

Add the string-first `WorkflowValue` envelope (`WorkflowValueKind`, `WorkflowValue`, `OutputPresentation`) to the public SDK surface. This is the presentation-aware contract every workflow node output is normalized to, powering the context-aware results interpreter in the studio.
