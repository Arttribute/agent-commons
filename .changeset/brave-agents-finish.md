---
'@agent-commons/sdk': minor
'@agent-commons/cli': minor
---

Make autonomous execution the default for local agent work, add caller-defined CLI tool catalogs to the SDK, fix local `--no-stream` runs so tool results can complete, and expand provider configuration for OpenRouter, xAI, Ollama, and custom OpenAI-compatible endpoints.
