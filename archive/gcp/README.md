# Archived Google Cloud deployment

Agent Commons production moved completely to AWS in June 2026.

This directory preserves the former Cloud Build and Cloud Run migration files
for reference only. They are not part of CI/CD and must not be attached to a
Google Cloud Build trigger.

Current production deployment: `.github/workflows/deploy-commons-api-aws.yml`.
