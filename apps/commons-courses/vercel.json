{
  "buildCommand": "pnpm build",
  "outputDirectory": ".next",
  "installCommand": "corepack enable && corepack prepare pnpm@10.34.5 --activate && cd ../.. && COREPACK_ENABLE_PROJECT_SPEC=0 pnpm install --no-frozen-lockfile",
  "framework": "nextjs"
}
