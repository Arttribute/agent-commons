import { LoadingScreen } from "@/components/loading-screen";

export default function AuthLoading() {
  return (
    <LoadingScreen
      title="Preparing sign-in"
      subtitle="Setting up a secure handoff."
    />
  );
}
