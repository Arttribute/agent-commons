# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Tool System Encryption Key
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Master encryption key for tool API keys and secrets
# REQUIRED: Generate a 256-bit (32-byte) key in hexadecimal format
#
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# IMPORTANT:
# - Keep this key secure and backed up
# - Never commit this to version control
# - If lost, all encrypted keys become unrecoverable
# - Rotate periodically for security
#
TOOL_KEY_ENCRYPTION_MASTER=your_64_character_hex_string_here_replace_with_generated_value

# Example (DO NOT USE THIS IN PRODUCTION):
# TOOL_KEY_ENCRYPTION_MASTER=a1b2c3d4e5f6789012345678901234567890123456789012345678901234abcd
